# TODO: Update StudentReportView.js to read data from ReportPage.js with student id

## Tasks
- [ ] Import `onValue` from firebase/database in StudentReportView.js
- [ ] Update the current term scores fetching to use `onValue` instead of `get` for real-time updates, matching ReportPage.js logic
- [ ] Ensure the data is fetched with the student id as before
- [ ] Test the changes to verify real-time updates work
