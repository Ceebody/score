import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yerielbrachaschool.academicdb',
  appName: 'academic-database',
  webDir: 'build'
};

export default config;
